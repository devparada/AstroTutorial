---
const {
  post: { title, description, content, created_at, id },
} = Astro.props;
---

<a href={`/posts/${id}`}>
<article class="bg-[#1F2937] text-white max-w-sm px-5 py-6">
  <img
    src="https://img.freepik.com/vector-gratis/piense-fuera-ilustracion-concepto-caja_114360-15734.jpg"
    alt=""
  />
  <h2 class="text-2xl font-bold">{title}</h2>
  <p>{description}</p>
  <p>{content.slice(0,100) + "..."}</p>
  <p>{new Date(created_at).toLocaleDateString()} - 14 minutos de lectura</p>
</article>
</a>
